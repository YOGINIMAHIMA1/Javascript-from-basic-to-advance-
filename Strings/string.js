var strName='MotherTeressa';
console.log(strName);
// string !== array  i.e string is not an array
console.log(strName.pop());//it will show an error as pop() cannot be performe don string as string is not an array
// string concatenation
var name='Lisa'
var sirName='Sharma'
console.log(name+ sirName);
console.log(name.concat(sirName))
